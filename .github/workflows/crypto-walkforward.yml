name: Crypto Structural Fast Tuning

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

env:
  PYTHONUNBUFFERED: "1"

jobs:
  walkforward:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout tuning branch
        uses: actions/checkout@v4
        with:
          ref: chatgpt-crypto-backtest-20260822
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy requests tabulate

      - name: Run focused structural regime tuning
        run: python backtests/crypto_structural_fast.py

      - name: Commit results to tuning branch
        if: always()
        run: |
          if [ -d structural_fast_output ] && [ "$(find structural_fast_output -type f | wc -l)" -gt 0 ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add structural_fast_output
            git commit -m "Store focused structural tuning results [skip ci]" || true
            git push origin HEAD:chatgpt-crypto-backtest-20260822
          fi

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: crypto-structural-fast-results
          path: structural_fast_output/
          if-no-files-found: warn
          retention-days: 7
